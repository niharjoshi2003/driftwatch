# Scraper Studio

Collector created from this repo via `brightdata scraper create` and `contracts/create_prompt.txt`.

Runs: Collection API `POST /dca/trigger` + `GET /dca/dataset`.

Heals: AI Flow via CLI `scraper heal` / `approve` / `approve --reject`.

Pin ID in `.cursor/rules/collector.mdc`. Do not reuse `c_msm3xrpc7r50hnnob`.
